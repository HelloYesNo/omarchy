#!/bin/bash

rfkill unblock wifi 2>/dev/null || true

if [[ -x ~/.local/bin/impala ]]; then
  IMPALA=~/.local/bin/impala
elif [[ -x ~/.cargo/bin/impala ]]; then
  IMPALA=~/.cargo/bin/impala
else
  IMPALA=impala
fi

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.omarchy.impala -e "$IMPALA"
